
// IronSight.App.UI.Views.AboutView.xaml
<UserControl x:Class="IronSight.App.UI.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">

    <Grid Background="#0A0A0C">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40,30">

                <!-- Header Section -->
                <Grid Margin="0,0,0,40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Width="70" Height="70" Background="#2563EB" CornerRadius="16">
                        <Path Data="M12,1L3,5V11C3,16.55 6.84,21.74 12,23C17.16,21.74 21,16.55 21,11V5L12,1M12,3L19,6.11V11C19,15.33 16.14,19.29 12,20.4C7.86,19.29 5,15.33 5,11V6.11L12,3M12,7A4,4 0 0,0 8,11A4,4 0 0,0 12,15A4,4 0 0,0 16,11A4,4 0 0,0 12,7M12,9A2,2 0 0,1 14,11A2,2 0 0,1 12,13A2,2 0 0,1 10,11A2,2 0 0,1 12,9Z" 
                              Fill="White" Stretch="Uniform" Margin="15"/>
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                        <TextBlock Text="ABOUT IRONSIGHT" FontSize="32" FontWeight="Black" Foreground="White"/>
                        <TextBlock Text="版本 1.0.4-Alpha | 高性能系统深度监控" Foreground="#64748B" FontSize="14" FontWeight="Medium"/>
                    </StackPanel>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left Content -->
                    <StackPanel Grid.Column="0" Margin="0,0,20,0">
                        <!-- Vision Section -->
                        <Border Background="#121215" BorderBrush="#1AFFFFFF" BorderThickness="1" CornerRadius="16" Padding="25" Margin="0,0,0,25">
                            <StackPanel>
                                <TextBlock Text="项目愿景" Foreground="#3B82F6" FontWeight="Bold" Margin="0,0,0,10"/>
                                <TextBlock TextWrapping="Wrap" Foreground="#94A3B8" FontSize="14" LineHeight="22">
                                    IronSight 旨在提供一个透明、高性能且现代化的 Windows 系统监控解决方案。
                                    通过深度调用底层 API，我们让每一个字节的流动都清晰可见。
                                    本项目由 <Run Foreground="White" FontWeight="Bold">Idealend Bin</Run> 与 <Run Foreground="White" FontWeight="Bold">Gemini</Run> 深度协作完成。
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <!-- License Matrix -->
                        <TextBlock Text="多层级授权架构" Foreground="#A855F7" FontWeight="Bold" Margin="0,0,0,15"/>

                        <!-- LGPL Item -->
                        <Border Background="#0DFFFFFF" CornerRadius="12" Padding="15" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="#1AFA8B3D" Width="40" Height="40" CornerRadius="8">
                                    <Path Data="M17,18L12,15.82L7,18V5H17M17,3H7A2,2 0 0,0 5,5V21L12,18L19,21V5C19,3.89 18.1,3 17,3Z" Fill="#FA8B3D" Stretch="Uniform" Margin="10"/>
                                </Border>
                                <StackPanel Grid.Column="1" Margin="15,0">
                                    <TextBlock Text="IronSight.Core.Native" Foreground="White" FontWeight="Bold"/>
                                    <TextBlock Text="内核模块。修改源码需回馈，允许动态链接。" Foreground="#64748B" FontSize="11" Margin="0,2,0,0"/>
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="LGPL v3" Foreground="#FA8B3D" VerticalAlignment="Center" FontWeight="Bold" FontFamily="Consolas"/>
                            </Grid>
                        </Border>

                        <!-- MPL Item -->
                        <Border Background="#0DFFFFFF" CornerRadius="12" Padding="15" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="#1A3B82F6" Width="40" Height="40" CornerRadius="8">
                                    <Path Data="M13,9V3.5L18.5,9M6,2C4.89,2 4,2.89 4,4V20C4,21.11 4.89,22 6,22H18C19.11,22 20,21.11 20,20V8L14,2H6Z" Fill="#3B82F6" Stretch="Uniform" Margin="10"/>
                                </Border>
                                <StackPanel Grid.Column="1" Margin="15,0">
                                    <TextBlock Text="IronSight.App.UI &amp; Interop" Foreground="White" FontWeight="Bold"/>
                                    <TextBlock Text="应用层。保护文件级源码，支持灵活集成。" Foreground="#64748B" FontSize="11" Margin="0,2,0,0"/>
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="MPL 2.0" Foreground="#3B82F6" VerticalAlignment="Center" FontWeight="Bold" FontFamily="Consolas"/>
                            </Grid>
                        </Border>

                        <!-- MIT Item -->
                        <Border Background="#0DFFFFFF" CornerRadius="12" Padding="15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Background="#1A22C55E" Width="40" Height="40" CornerRadius="8">
                                    <Path Data="M21,16.5C21,16.88 20.79,17.21 20.47,17.38L12.57,21.82C12.41,21.94 12.21,22 12,22C11.79,22 11.59,21.94 11.43,21.82L3.53,17.38C3.21,17.21 3,16.88 3,16.5V7.5C3,7.12 3.21,6.79 3.53,6.62L11.43,2.18C11.59,2.06 11.79,2 12,2C12.21,2 12.41,2.06 12.57,2.18L20.47,6.62C20.79,6.79 21,7.12 21,7.5V16.5Z" Fill="#22C55E" Stretch="Uniform" Margin="10"/>
                                </Border>
                                <StackPanel Grid.Column="1" Margin="15,0">
                                    <TextBlock Text="IronSight.Extension.*" Foreground="White" FontWeight="Bold"/>
                                    <TextBlock Text="SDK 与插件。极致开放，鼓励生态开发。" Foreground="#64748B" FontSize="11" Margin="0,2,0,0"/>
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="MIT" Foreground="#22C55E" VerticalAlignment="Center" FontWeight="Bold" FontFamily="Consolas"/>
                            </Grid>
                        </Border>
                    </StackPanel>

                    <!-- Right Sidebar -->
                    <StackPanel Grid.Column="1">
                        <Border CornerRadius="16" Padding="20" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="开发协作" Foreground="White" FontWeight="Bold" Margin="0,0,0,15"/>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <Ellipse Width="8" Height="8" Fill="#3B82F6" Margin="0,0,10,0"/>
                                    <StackPanel>
                                        <TextBlock Text="Idealend Bin" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                                        <TextBlock Text="Native Wrapper与事件集成" Foreground="#94A3B8" FontSize="10"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="8" Height="8" Fill="#A855F7" Margin="0,0,10,0"/>
                                    <StackPanel>
                                        <TextBlock Text="Gemini" Foreground="White" FontSize="12" FontWeight="SemiBold"/>
                                        <TextBlock Text="界面设计与上层代码" Foreground="#94A3B8" FontSize="10"/>
                                    </StackPanel>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Button Content="访问 GitHub 仓库" Background="#FFFFFF" Foreground="Black" FontWeight="Bold" Height="40" BorderThickness="0">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="10"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>
                </Grid>

                <Separator Background="#1AFFFFFF" Margin="0,40,0,20"/>

                <TextBlock Text="DESIGNED FOR TRANSPARENCY. BUILT FOR PERFORMANCE." 
                           HorizontalAlignment="Center" Foreground="#334155" FontSize="10" FontWeight="Bold"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

// IronSight.App.UI.Views.ClipboardView
<UserControl x:Class="IronSight.App.UI.Views.ClipboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IronSight.App.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,28">
            <TextBlock Text="云端剪贴板" 
                                           FontSize="32" 
                                           FontWeight="SemiBold" 
                                           Foreground="{StaticResource TextPrimary}"/>
            <TextBlock Text="您的剪贴板历史记录，跨设备同步" 
                                           FontSize="14" 
                                           Foreground="{StaticResource TextSecondary}"
                                           Margin="0,8,0,0"/>
        </StackPanel>

        <ListBox Grid.Row="1" 
                                     ItemsSource="{Binding ClipboardHistory}" 
                                     Background="Transparent" 
                                     BorderThickness="0"
                                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Margin" Value="0,0,0,12"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Border x:Name="ItemBorder" 
                                                            Style="{StaticResource FluentCard}"
                                                            Padding="20">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding DisplayText}" 
                                                                           FontSize="14" 
                                                                           Foreground="{StaticResource TextPrimary}" 
                                                                           TextTrimming="CharacterEllipsis"
                                                                           FontWeight="Medium"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                                <TextBlock Text="&#xE823;" 
                                                                               FontFamily="Segoe Fluent Icons" 
                                                                               FontSize="11"
                                                                               Foreground="{StaticResource TextTertiary}"/>
                                                <TextBlock Text="{Binding Timestamp, StringFormat=HH:mm:ss}" 
                                                                               FontSize="12" 
                                                                               Foreground="{StaticResource TextTertiary}" 
                                                                               Margin="6,0,0,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <Button Command="{Binding DataContext.CopyClipboardItemCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                                    CommandParameter="{Binding}"
                                                                    Grid.Column="1"     
                                                                    Content="复制" 
                                                                    Style="{StaticResource SecondaryButton}"/>
                                    </Grid>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</UserControl>

// IronSight.App.UI.Views.DashboardView
<UserControl x:Class="IronSight.App.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IronSight.App.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Disabled">
        <ScrollViewer.Resources>
            <Style TargetType="ScrollBar" BasedOn="{StaticResource FluentScrollBar}"/>
        </ScrollViewer.Resources>
        <StackPanel>
            <!-- 页面标题 -->
            <StackPanel Margin="0,0,0,32">
                <TextBlock Text="系统监控" 
                                               FontSize="32" 
                                               FontWeight="SemiBold" 
                                               Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="实时监控您的系统性能与资源使用情况" 
                                               FontSize="14" 
                                               Foreground="{StaticResource TextSecondary}"
                                               Margin="0,8,0,0"/>
            </StackPanel>

            <!-- 状态卡片组 -->
            <UniformGrid Columns="3" Margin="0,0,0,24">
                <!-- CPU卡片 -->
                <Border Style="{StaticResource FluentCard}" Margin="0,0,12,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="36" Height="36" CornerRadius="8" 
                                                            Background="#2060CDFF">
                                    <TextBlock Text="&#xE950;" 
                                                                   FontFamily="Segoe Fluent Icons" 
                                                                   FontSize="16" 
                                                                   Foreground="{StaticResource AccentBrush}"
                                                                   HorizontalAlignment="Center" 
                                                                   VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="CPU 使用率" 
                                                                   Foreground="{StaticResource TextSecondary}" 
                                                                   FontSize="12"/>
                                    <TextBlock Text="处理器" 
                                                                   Foreground="{StaticResource TextTertiary}" 
                                                                   FontSize="11"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Row="1" Margin="0,20,0,0">
                            <TextBlock Text="{Binding CpuUsage, StringFormat={}{0:F1}}" 
                                                           FontSize="42" 
                                                           FontWeight="SemiBold" 
                                                           Foreground="{StaticResource TextPrimary}">
                                <Run Text="%" FontSize="24" Foreground="{StaticResource TextSecondary}"/>
                            </TextBlock>
                        </StackPanel>

                        <ProgressBar Grid.Row="2" 
                                                         Value="{Binding CpuUsage}" 
                                                         Style="{StaticResource FluentProgressBar}" 
                                                         Margin="0,16,0,0"/>
                    </Grid>
                </Border>

                <!-- 磁盘读取卡片 -->
                <Border Style="{StaticResource FluentCard}" Margin="6,0,6,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="36" Height="36" CornerRadius="8" 
                                                            Background="#206CCB5F">
                                    <TextBlock Text="&#xE896;" 
                                                                   FontFamily="Segoe Fluent Icons" 
                                                                   FontSize="16" 
                                                                   Foreground="{StaticResource SuccessBrush}"
                                                                   HorizontalAlignment="Center" 
                                                                   VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="磁盘读取" 
                                                                   Foreground="{StaticResource TextSecondary}" 
                                                                   FontSize="12"/>
                                    <TextBlock Text="传输速率" 
                                                                   Foreground="{StaticResource TextTertiary}" 
                                                                   FontSize="11"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Row="1" Margin="0,20,0,0">
                            <TextBlock FontSize="42" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}">
                                <Run Text="{Binding DiskReadRate, StringFormat={}{0:F1}}"/>
                                <Run Text="MB/s" FontSize="16" Foreground="{StaticResource TextSecondary}"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0">
                            <Ellipse Width="8" Height="8" Fill="{StaticResource SuccessBrush}"/>
                            <TextBlock Text="稳定运行中" 
                                                           FontSize="12" 
                                                           Foreground="{StaticResource SuccessBrush}"
                                                           Margin="8,0,0,0"
                                                           VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 磁盘写入卡片 -->
                <Border Style="{StaticResource FluentCard}" Margin="12,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="36" Height="36" CornerRadius="8" 
                                                            Background="#20FCE100">
                                    <TextBlock Text="&#xE898;" 
                                                                   FontFamily="Segoe Fluent Icons" 
                                                                   FontSize="16" 
                                                                   Foreground="{StaticResource WarningBrush}"
                                                                   HorizontalAlignment="Center" 
                                                                   VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="磁盘写入" 
                                                                   Foreground="{StaticResource TextSecondary}" 
                                                                   FontSize="12"/>
                                    <TextBlock Text="传输速率" 
                                                                   Foreground="{StaticResource TextTertiary}" 
                                                                   FontSize="11"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Row="1" Margin="0,20,0,0">
                            <TextBlock FontSize="42" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}">
                                <Run Text="{Binding DiskWriteRate, StringFormat={}{0:F1}}"/>
                                <Run Text="MB/s" FontSize="16" Foreground="{StaticResource TextSecondary}"/>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,16,0,0">
                            <Ellipse Width="8" Height="8" Fill="{StaticResource SuccessBrush}"/>
                            <TextBlock Text="队列正常" 
                                                           FontSize="12" 
                                                           Foreground="{StaticResource SuccessBrush}"
                                                           Margin="8,0,0,0"
                                                           VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </UniformGrid>

            <!-- 系统日志卡片 -->
            <Border Style="{StaticResource FluentCard}">
                <StackPanel>
                    <Grid Margin="0,0,0,16">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE7BA;" 
                                                           FontFamily="Segoe Fluent Icons" 
                                                           FontSize="16" 
                                                           Foreground="{StaticResource AccentBrush}"
                                                           VerticalAlignment="Center"/>
                            <TextBlock Text="系统日志" 
                                                           FontWeight="SemiBold" 
                                                           FontSize="16"
                                                           Foreground="{StaticResource TextPrimary}" 
                                                           Margin="12,0,0,0"/>
                        </StackPanel>
                        <Button Content="清除" 
                                                    Style="{StaticResource SecondaryButton}" 
                                                    HorizontalAlignment="Right"
                                                    Padding="12,6"
                                                    Click="Clear_Click"/>
                    </Grid>

                    <Border Background="#12FFFFFF" CornerRadius="8" Padding="16">
                        <TextBlock Text="{Binding StatusMessage}" 
                                                       Foreground="{StaticResource TextSecondary}" 
                                                       TextWrapping="Wrap" 
                                                       LineHeight="24" 
                                                       FontFamily="Cascadia Code, Consolas, Courier New"
                                                       FontSize="13"/>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>

// IronSight.App.UI.Views.MemoryCleanerView
<UserControl x:Class="IronSight.App.UI.Views.MemoryCleanerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:IronSight.App.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="400">
            <!-- 动画图标 -->
            <Border Width="120" Height="120" CornerRadius="60" Margin="0,0,0,40">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#2060CDFF" Offset="0"/>
                        <GradientStop Color="#100078D4" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect Color="{StaticResource AccentColorValue}" 
                                                          BlurRadius="40" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>
                <TextBlock Text="&#xE950;" 
                                               FontFamily="Segoe Fluent Icons" 
                                               FontSize="48" 
                                               Foreground="{StaticResource AccentBrush}" 
                                               HorizontalAlignment="Center" 
                                               VerticalAlignment="Center"/>
            </Border>

            <TextBlock Text="内存智能优化" 
                                           FontSize="28" 
                                           FontWeight="SemiBold" 
                                           Foreground="{StaticResource TextPrimary}" 
                                           HorizontalAlignment="Center"/>

            <TextBlock Text="回收未使用的进程工作集，提升系统响应速度。&#x0a;此操作安全且不会影响正在运行的程序。" 
                                           Foreground="{StaticResource TextSecondary}" 
                                           Margin="0,16,0,40" 
                                           HorizontalAlignment="Center"
                                           TextAlignment="Center"
                                           LineHeight="24"
                                           FontSize="14"/>

            <Button Command="{Binding CleanMemoryCommand}" 
                                        Style="{StaticResource PrimaryButton}"
                                        HorizontalAlignment="Center"
                                        Padding="40,14">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE768;" 
                                                   FontFamily="Segoe Fluent Icons" 
                                                   FontSize="16" 
                                                   VerticalAlignment="Center"/>
                    <TextBlock Text="立即深度清理" 
                                                   Margin="12,0,0,0" 
                                                   FontSize="15"
                                                   FontWeight="SemiBold"/>
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>

// IronSight.App.UI.Views.NetworkMonitorView
<UserControl x:Class="IronSight.App.UI.Views.NetworkMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:IronSight.App.UI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="#1E1E1E">
    <UserControl.DataContext>
        <vm:NetworkMonitorViewModel/>
    </UserControl.DataContext>
    
    <UserControl.Resources>
        <!-- 基础样式 -->
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="CellTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#AAAAAA"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>

        <!-- DataGrid样式 -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#333333"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#252525"/>
            <Setter Property="AlternatingRowBackground" Value="#2A2A2A"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#333333"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#2D2D2D"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#0078D4"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- 按钮样式 -->
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3C3C3C"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4C4C4C"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0078D4"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox样式 -->
        <Style x:Key="FilterCheckBoxStyle" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <!-- TextBox样式 -->
        <Style x:Key="SearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="#CCCCCC"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 工具栏 -->
        <Border Grid.Row="0" 
                Background="#2D2D2D" 
                Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧按钮 -->
                <StackPanel Grid.Column="0" 
                            Orientation="Horizontal">
                    <Button Content="⟳ Refresh"
                            Style="{StaticResource ToolbarButtonStyle}"
                            Command="{Binding RefreshCommand}"
                            IsEnabled="{Binding IsNotRefreshing, Mode=OneWay}"
                            Click="OnRefreshClick"/>

                    <ToggleButton Content="▶ Auto"
                                  IsChecked="{Binding IsAutoRefreshEnabled}"
                                  Margin="0,0,8,0"
                                  Padding="12,6"
                                  Background="#3C3C3C"
                                  Foreground="#CCCCCC"
                                  BorderThickness="0"/>
                </StackPanel>

                <!-- 中间过滤器 -->
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal"
                            HorizontalAlignment="Center">
                    <CheckBox Content="TCP"
                              IsChecked="{Binding ShowTcp}"
                              Style="{StaticResource FilterCheckBoxStyle}"/>
                    <CheckBox Content="UDP"
                              IsChecked="{Binding ShowUdp}"
                              Style="{StaticResource FilterCheckBoxStyle}"/>
                </StackPanel>

                <!-- 右侧搜索框 -->
                <TextBox Grid.Column="2"
                         Width="250"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource SearchTextBoxStyle}">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </Grid>
        </Border>

        <!-- 刷新间隔设置 -->
        <Border Grid.Row="1"
                Background="#252525"
                Padding="12,6">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Refresh Interval:"
                           Foreground="#888888"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"/>
                <Slider Width="150"
                        Minimum="100"
                        Maximum="5000"
                        Value="{Binding RefreshInterval}"
                        VerticalAlignment="Center"/>
                <TextBlock Text="{Binding RefreshInterval, StringFormat={}{0:N0} ms}"
                           Foreground="#888888"
                           VerticalAlignment="Center"
                           Margin="8,0,0,0"
                           Width="60"/>
            </StackPanel>
        </Border>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding Connections}"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  ScrollViewer.IsDeferredScrollingEnabled="True">
            <DataGrid.Columns>
                <!-- 协议 -->
                <DataGridTemplateColumn Header="Protocol" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource StatusBadgeStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Protocol}" Value="TCP">
                                                <Setter Property="Background" Value="#2D5A2D"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Protocol}" Value="UDP">
                                                <Setter Property="Background" Value="#5A5A2D"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Protocol}"
                                           Foreground="White"
                                           FontSize="10"
                                           FontWeight="Bold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- 本地地址 -->
                <DataGridTextColumn Header="Local Address"
                                    Binding="{Binding LocalAddress}"
                                    Width="140"
                                    ElementStyle="{StaticResource CellTextStyle}"/>

                <!-- 本地端口 -->
                <DataGridTextColumn Header="Local Port"
                                    Binding="{Binding LocalPort}"
                                    Width="90"
                                    ElementStyle="{StaticResource CellTextStyle}"/>

                <!-- 远程地址 -->
                <DataGridTextColumn Header="Remote Address"
                                    Binding="{Binding RemoteAddress}"
                                    Width="140"
                                    ElementStyle="{StaticResource CellTextStyle}"/>

                <!-- 远程端口 -->
                <DataGridTextColumn Header="Remote Port"
                                    Binding="{Binding RemotePort}"
                                    Width="90"
                                    ElementStyle="{StaticResource CellTextStyle}"/>

                <!-- 状态 -->
                <DataGridTemplateColumn Header="State" Width="110">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border>
                                <Border.Style>
                                    <Style TargetType="Border" BasedOn="{StaticResource StatusBadgeStyle}">
                                        <Setter Property="Background" Value="#444444"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding State}" Value="Established">
                                                <Setter Property="Background" Value="#1B5E20"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding State}" Value="Listening">
                                                <Setter Property="Background" Value="#0D47A1"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding State}" Value="Time Wait">
                                                <Setter Property="Background" Value="#E65100"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding State}" Value="Close Wait">
                                                <Setter Property="Background" Value="#B71C1C"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding State}"
                                           Foreground="White"
                                           FontSize="10"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- PID -->
                <DataGridTextColumn Header="PID"
                                    Binding="{Binding ProcessId}"
                                    Width="70"
                                    ElementStyle="{StaticResource CellTextStyle}"/>

                <!-- 进程名 -->
                <DataGridTextColumn Header="Process Name"
                                    Binding="{Binding ProcessName}"
                                    Width="*"
                                    ElementStyle="{StaticResource CellTextStyle}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 状态栏 -->
        <Border Grid.Row="3"
                Background="#2D2D2D"
                Padding="12,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧统计 -->
                <StackPanel Grid.Column="0" 
                            Orientation="Horizontal">
                    <TextBlock Foreground="#888888" Margin="0,0,20,0">
                        <Run Text="Total: "/>
                        <Run Text="{Binding TotalConnectionCount, Mode=OneWay}" Foreground="#CCCCCC"/>
                    </TextBlock>
                    <TextBlock Foreground="#888888" Margin="0,0,20,0">
                        <Run Text="TCP: "/>
                        <Run Text="{Binding TcpConnectionCount, Mode=OneWay}" Foreground="#4CAF50"/>
                    </TextBlock>
                    <TextBlock Foreground="#888888">
                        <Run Text="UDP: "/>
                        <Run Text="{Binding UdpConnectionCount, Mode=OneWay}" Foreground="#FFC107"/>
                    </TextBlock>
                </StackPanel>

                <!-- 右侧状态 -->
                <StackPanel Grid.Column="1" 
                            Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" 
                             Margin="0,0,6,0"
                             VerticalAlignment="Center">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#4CAF50"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRefreshing, Mode=OneWay}" Value="True">
                                        <Setter Property="Fill" Value="#FFC107"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Foreground="#888888">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Ready"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRefreshing, Mode=OneWay}" Value="True">
                                        <Setter Property="Text" Value="Refreshing..."/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>

// IronSight.App.UI.Views.ProcessManagerView
<UserControl x:Class="IronSight.App.UI.Views.ProcessManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:IronSight.App.UI.ViewModels"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900"
             FontFamily="Segoe UI Variable Display, Segoe UI, sans-serif">

    <UserControl.Resources>
        <!-- 搜索框样式：更圆润，集成 Fluent 焦点效果 -->
        <Style x:Key="FluentSearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderCard}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="8">
                            <!-- Fluent 圆角 -->
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <!-- 搜索图标 -->
                                <TextBlock Grid.Column="0" Text="&#xE721;" 
                                           FontFamily="Segoe Fluent Icons" 
                                           Foreground="{StaticResource TextTertiary}"
                                           VerticalAlignment="Center" Margin="12,0,4,0" FontSize="14"/>

                                <ScrollViewer Grid.Column="1" x:Name="PART_ContentHost" VerticalAlignment="Center"/>

                                <!-- Placeholder -->
                                <TextBlock Grid.Column="1" Text="{TemplateBinding Tag}" 
                                           Foreground="{StaticResource TextDisabled}"
                                           VerticalAlignment="Center" Margin="2,0,0,0"
                                           IsHitTestVisible="False">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource CardHoverBackground}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource BorderHover}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource CardBackground}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- 危险按钮 Fluent 风格 (带阴影和动效) -->
        <Style x:Key="FluentDangerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <!-- 默认微弱阴影 -->
                            <Border.Effect>
                                <DropShadowEffect Color="{StaticResource DangerColorValue}" BlurRadius="8" ShadowDepth="0" Opacity="0.2"/>
                            </Border.Effect>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="&#xE74D;" FontFamily="Segoe Fluent Icons" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <!-- 悬停变亮并增加光晕 -->
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#FF6B6B"/>
                                <!-- Adjust slightly lighter manually or use binding -->
                                <Setter TargetName="ButtonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{StaticResource DangerColorValue}" BlurRadius="12" ShadowDepth="0" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#CC4444"/>
                                <Setter TargetName="ButtonBorder" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="{StaticResource DangerColorValue}" BlurRadius="4" ShadowDepth="0" Opacity="0.2"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" CenterX="50" CenterY="15"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.4"/>
                                <Setter TargetName="ButtonBorder" Property="Effect" Value="{x:Null}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource PrimaryBackground}">
        <Grid.DataContext>
            <vm:ProcessViewModel/>
        </Grid.DataContext>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ================= 顶部工具栏 ================= -->
        <Border Grid.Row="0" 
                Background="{StaticResource SecondaryBackground}" 
                BorderBrush="{StaticResource BorderSubtle}" 
                BorderThickness="0,0,0,1" 
                Padding="24,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox Style="{StaticResource FluentSearchBoxStyle}"
                         Width="320" HorizontalAlignment="Left"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Tag="搜索进程名称或 PID..."/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" >
                    <!-- 刷新按钮 (示例，可选) -->
                    <Button Style="{StaticResource SecondaryButton}" 
                            Content="&#xE72C;" FontFamily="Segoe Fluent Icons"
                            ToolTip="刷新" Padding="10,6" FontSize="14"/>

                    <Button Content="结束进程" 
                            Command="{Binding TerminateProcessCommand}"
                            Style="{StaticResource FluentDangerButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ================= 数据表格 ================= -->
        <!-- 使用 Grid 包裹以添加内边距或背景微调 -->
        <Grid Grid.Row="1" Margin="0,0,0,0">
            <DataGrid ItemsSource="{Binding ProcessView}" 
                      SelectedItem="{Binding SelectedProcess}"
                      AutoGenerateColumns="False" 
                      IsReadOnly="True" 
                      Background="Transparent" 
                      BorderThickness="0"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="{StaticResource BorderSubtle}"
                      HeadersVisibility="Column"
                      RowHeight="40"
                      FontSize="13"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      Style="{x:Null}">
                <!-- 清除默认样式 -->

                <!-- 应用 Fluent 滚动条 -->
                <DataGrid.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource FluentScrollBar}"/>

                    <!-- 表头样式 -->
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{StaticResource PrimaryBackground}"/>
                        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                        <Setter Property="Padding" Value="12,10"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtle}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridColumnHeader">
                                    <Border Background="{TemplateBinding Background}" 
                                            BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                          VerticalAlignment="Center" 
                                                          Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                    <!-- 行样式 -->
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Tag" Value="{Binding DataContext, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
                        <Setter Property="ContextMenu">
                            <Setter.Value>
                                <ContextMenu>
                                    <MenuItem Header="结束任务" 
                                              InputGestureText="Del"
                                              Command="{Binding PlacementTarget.Tag.TerminateProcessCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}"
                                              Foreground="{StaticResource DangerBrush}">
                                        <MenuItem.Icon>
                                            <TextBlock Text="&#xE74D;" FontFamily="Segoe Fluent Icons" Foreground="{StaticResource DangerBrush}"/>
                                        </MenuItem.Icon>
                                    </MenuItem>
                                    <Separator Style="{StaticResource FluentSeparator}" Margin="0,4"/>
                                    <MenuItem Header="设置优先级">
                                        <MenuItem Header="实时" Command="{Binding PlacementTarget.Tag.SetPriorityCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="Realtime"/>
                                        <MenuItem Header="高" Command="{Binding PlacementTarget.Tag.SetPriorityCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="High"/>
                                        <MenuItem Header="高于正常" Command="{Binding PlacementTarget.Tag.SetPriorityCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="AboveNormal"/>
                                        <MenuItem Header="正常" Command="{Binding PlacementTarget.Tag.SetPriorityCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="Normal"/>
                                        <MenuItem Header="低于正常" Command="{Binding PlacementTarget.Tag.SetPriorityCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="BelowNormal"/>
                                        <MenuItem Header="低" Command="{Binding PlacementTarget.Tag.SetPriorityCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" CommandParameter="Low"/>
                                    </MenuItem>
                                    <Separator Style="{StaticResource FluentSeparator}" Margin="0,4"/>
                                    <MenuItem Header="打开文件所在位置" 
                                              Command="{Binding PlacementTarget.Tag.OpenLocationCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                        <MenuItem.Icon>
                                            <TextBlock Text="&#xE8DA;" FontFamily="Segoe Fluent Icons"/>
                                        </MenuItem.Icon>
                                    </MenuItem>
                                    <MenuItem Header="属性" 
                                              Command="{Binding PlacementTarget.Tag.ShowPropertiesCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}">
                                        <MenuItem.Icon>
                                            <TextBlock Text="&#xE946;" FontFamily="Segoe Fluent Icons"/>
                                        </MenuItem.Icon>
                                    </MenuItem>
                                </ContextMenu>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource CardHoverBackground}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <!-- 使用半透明 Accent 色作为选中背景，保持文字清晰 -->
                                <Setter Property="Background" Value="#2560CDFF"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- 单元格样式：移除焦点边框 -->
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="12,0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <!-- PID: 弱化显示 -->
                    <DataGridTextColumn Header="PID" Binding="{Binding Pid}" Width="80" 
                                        ElementStyle="{StaticResource {x:Type TextBlock}}">
                    </DataGridTextColumn>

                    <!-- 进程名: 强调显示 -->
                    <DataGridTemplateColumn Header="进程名称" Width="2*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="20" Height="20" CornerRadius="4" 
                                            Background="Transparent" Margin="0,0,12,0">
                                        <!-- 这里可以替换为真实的 Process Icon -->
                                        <TextBlock Text="&#xE770;" FontFamily="Segoe Fluent Icons" 
                                                   HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                   FontSize="12" Foreground="{StaticResource AccentBrush}"/>
                                    </Border>
                                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="Medium"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- CPU: 右对齐，数字字体 -->
                    <DataGridTemplateColumn Header="CPU" Width="100" SortMemberPath="CpuUsage">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding CpuUsage, StringFormat={}{0:F1}%}" 
                                           HorizontalAlignment="Right" Margin="0,0,12,0"
                                           FontFamily="Segoe UI Variable Display">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            <!-- 条件格式：高 CPU 标红 -->
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CpuUsage}" Value="0">
                                                    <Setter Property="Foreground" Value="{StaticResource TextDisabled}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <!-- 内存: 右对齐 -->
                    <DataGridTextColumn Header="内存 (私有)" Binding="{Binding MemoryMB, StringFormat={}{0:N1} MB}" Width="120">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="HorizontalAlignment" Value="Right"/>
                                <Setter Property="Margin" Value="0,0,12,0"/>
                                <Setter Property="FontFamily" Value="Segoe UI Variable Display"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- 磁盘: 视觉优化 -->
                    <DataGridTemplateColumn Header="磁盘 (R/W)" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,12,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding DiskReadRateMS, StringFormat={}{0:F1}}" 
                                               Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Right"/>
                                    <TextBlock Grid.Column="1" Text="/" Foreground="{StaticResource TextDisabled}" Margin="5,0"/>
                                    <TextBlock Grid.Column="2" Text="{Binding DiskWriteRateMS, StringFormat={}{0:F1}}" 
                                               Foreground="{StaticResource WarningBrush}" HorizontalAlignment="Left"/>
                                </Grid>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="句柄" Binding="{Binding HandleCount}" Width="80" Foreground="{StaticResource TextTertiary}"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- ================= 底部状态栏 ================= -->
        <Border Grid.Row="2" 
                Background="{StaticResource SecondaryBackground}" 
                BorderBrush="{StaticResource BorderSubtle}"
                BorderThickness="0,1,0,0"
                Padding="16,8">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="0,0,24,0">
                        <TextBlock Text="&#xE71D;" FontFamily="Segoe Fluent Icons" Foreground="{StaticResource TextTertiary}" Margin="0,1,8,0"/>
                        <TextBlock Text="进程: " Foreground="{StaticResource TextSecondary}" FontSize="12"/>
                        <TextBlock Text="{Binding ProcessCount}" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" Margin="4,0,0,0" FontSize="12"/>
                    </StackPanel>

                    <Rectangle Width="1" Height="14" Fill="{StaticResource BorderSubtle}" Margin="0,0,24,0"/>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="全局句柄: " Foreground="{StaticResource TextSecondary}" FontSize="12"/>
                        <TextBlock Text="{Binding TotalHandleCount}" Foreground="{StaticResource TextPrimary}" Margin="4,0,0,0" FontSize="12"/>
                    </StackPanel>
                </StackPanel>

                <!-- 可选：右下角小标识 -->
                <TextBlock Text="IronSight Monitor" HorizontalAlignment="Right" 
                           Foreground="{StaticResource TextDisabled}" FontSize="11" 
                           VerticalAlignment="Center"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>

// IronSight.App.UI.Views.SettingsView
<UserControl x:Class="IronSight.App.UI.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">

    <Grid Background="#0A0A0C">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40,30">

                <!-- Page Header -->
                <StackPanel Margin="0,0,0,30">
                    <TextBlock Text="SETTINGS" FontSize="32" FontWeight="Black" Foreground="White"/>
                    <TextBlock Text="配置您的 IronSight 工作环境" Foreground="#64748B" FontSize="14"/>
                </StackPanel>

                <!-- General Settings Group -->
                <TextBlock Text="通用设置" Foreground="#3B82F6" FontWeight="Bold" Margin="0,0,0,15"/>
                <Border Background="#121215" CornerRadius="12" BorderBrush="#1AFFFFFF" BorderThickness="1" Padding="5">
                    <StackPanel>
                        <!-- Language Select -->
                        <Grid Margin="15,10">
                            <StackPanel>
                                <TextBlock Text="显示语言 (Language)" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Text="更改界面的主要显示语言" Foreground="#64748B" FontSize="12"/>
                            </StackPanel>
                            <ComboBox HorizontalAlignment="Right" Width="150" Height="32" VerticalContentAlignment="Center"
                                      Background="#1AFFFFFF" Foreground="White" BorderThickness="0">
                                <ComboBoxItem Content="简体中文" IsSelected="True"/>
                                <ComboBoxItem Content="English"/>
                            </ComboBox>
                        </Grid>

                        <Separator Background="#0DFFFFFF" Margin="15,0"/>

                        <!-- Appearance Mode -->
                        <Grid Margin="15,10">
                            <StackPanel>
                                <TextBlock Text="主题模式" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Text="跟随系统或手动指定主题" Foreground="#64748B" FontSize="12"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <RadioButton Content="浅色" Foreground="#94A3B8" Margin="0,0,15,0" VerticalAlignment="Center"/>
                                <RadioButton Content="深色" Foreground="#94A3B8" IsChecked="True" VerticalAlignment="Center"/>
                                <RadioButton Content="系统" Foreground="#94A3B8" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Performance Settings Group -->
                <TextBlock Text="性能与采样" Foreground="#A855F7" FontWeight="Bold" Margin="0,30,0,15"/>
                <Border Background="#121215" CornerRadius="12" BorderBrush="#1AFFFFFF" BorderThickness="1" Padding="5">
                    <StackPanel>
                        <!-- Sampling Frequency -->
                        <Grid Margin="15,10">
                            <StackPanel>
                                <TextBlock Text="数据采样频率 (ms)" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Text="更短的周期意味着更高的 CPU 消耗" Foreground="#64748B" FontSize="12"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <Slider Width="200" Minimum="100" Maximum="5000" Value="1000" VerticalAlignment="Center" x:Name="FreqSlider"/>
                                <TextBlock Text="{Binding ElementName=FreqSlider, Path=Value, StringFormat={}{0:0}ms}" 
                                           Foreground="#A855F7" Width="60" Margin="10,0,0,0" VerticalAlignment="Center" FontWeight="Bold"/>
                            </StackPanel>
                        </Grid>

                        <Separator Background="#0DFFFFFF" Margin="15,0"/>

                        <!-- Auto-Start -->
                        <Grid Margin="15,10">
                            <StackPanel>
                                <TextBlock Text="开机启动" Foreground="White" FontWeight="SemiBold"/>
                                <TextBlock Text="在 Windows 登录时自动启动监控" Foreground="#64748B" FontSize="12"/>
                            </StackPanel>
                            <CheckBox HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource {x:Static ToolBar.CheckBoxStyleKey}}"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Data Management -->
                <TextBlock Text="数据管理" Foreground="#F59E0B" FontWeight="Bold" Margin="0,30,0,15"/>
                <Border Background="#121215" CornerRadius="12" BorderBrush="#1AFFFFFF" BorderThickness="1" Padding="20">
                    <StackPanel>
                        <TextBlock Text="由于 IronSight 采用 ElementRuntime 高性能架构，原始网络封包在内存中按环形缓冲区存储。" 
                                   Foreground="#94A3B8" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,15"/>
                        <Button Content="立即清除历史流量统计" Background="#1AEE4444" Foreground="#FF5F5F" 
                                BorderBrush="#4DFF5F5F" BorderThickness="1" Height="35" HorizontalAlignment="Left" Padding="15,0"/>
                    </StackPanel>
                </Border>

                <!-- Bottom Action -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,40,0,20">
                    <Button Content="重置默认值" Background="Transparent" Foreground="#64748B" Margin="0,0,15,0" BorderThickness="0"/>
                    <Button Content="保存并应用" Background="#3B82F6" Foreground="White" FontWeight="Bold" Padding="30,8" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

// IronSight.App.UI.Resources.Styles.xaml
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- ==================== 核心色彩系统 ==================== -->
    <Color x:Key="PrimaryBackgroundColor">#0D0D0D</Color>
    <Color x:Key="SecondaryBackgroundColor">#141414</Color>
    <Color x:Key="TertiaryBackgroundColor">#1A1A1A</Color>
    <Color x:Key="CardBackgroundColor">#1F1F1F</Color>
    <Color x:Key="CardHoverBackgroundColor">#262626</Color>
    <Color x:Key="AccentColorValue">#60CDFF</Color>
    <Color x:Key="AccentSecondaryColorValue">#0078D4</Color>
    <Color x:Key="SuccessColorValue">#6CCB5F</Color>
    <Color x:Key="WarningColorValue">#FCE100</Color>
    <Color x:Key="DangerColorValue">#FF6B6B</Color>

    <!-- 画刷定义 -->
    <SolidColorBrush x:Key="PrimaryBackground" Color="{StaticResource PrimaryBackgroundColor}"/>
    <SolidColorBrush x:Key="SecondaryBackground" Color="{StaticResource SecondaryBackgroundColor}"/>
    <SolidColorBrush x:Key="TertiaryBackground" Color="{StaticResource TertiaryBackgroundColor}"/>
    <SolidColorBrush x:Key="CardBackground" Color="{StaticResource CardBackgroundColor}"/>
    <SolidColorBrush x:Key="CardHoverBackground" Color="{StaticResource CardHoverBackgroundColor}"/>

    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColorValue}"/>
    <SolidColorBrush x:Key="AccentSecondaryBrush" Color="{StaticResource AccentSecondaryColorValue}"/>
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColorValue}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColorValue}"/>
    <SolidColorBrush x:Key="DangerBrush" Color="{StaticResource DangerColorValue}"/>

    <!-- 文字颜色 -->
    <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#9CA3AF"/>
    <SolidColorBrush x:Key="TextTertiary" Color="#6B7280"/>
    <SolidColorBrush x:Key="TextDisabled" Color="#4B5563"/>

    <!-- 边框与分隔 -->
    <SolidColorBrush x:Key="BorderSubtle" Color="#2A2A2A"/>
    <SolidColorBrush x:Key="BorderCard" Color="#333333"/>
    <SolidColorBrush x:Key="BorderHover" Color="#404040"/>

    <!-- 渐变效果 -->
    <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#60CDFF" Offset="0"/>
        <GradientStop Color="#0078D4" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="CardGlowGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#0AFFFFFF" Offset="0"/>
        <GradientStop Color="#00FFFFFF" Offset="1"/>
    </LinearGradientBrush>

    <!-- ==================== 导航按钮样式 ==================== -->
    <Style x:Key="NavButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
        <Setter Property="Height" Value="44"/>
        <Setter Property="Margin" Value="8,2"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Padding" Value="14,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="RootBorder" Background="{TemplateBinding Background}" 
                                CornerRadius="8" BorderThickness="1" BorderBrush="Transparent">
                        <Grid>
                            <!-- 选中指示器 -->
                            <Rectangle x:Name="SelectionIndicator" 
                                           Fill="{StaticResource AccentBrush}" 
                                           HorizontalAlignment="Left" 
                                           Width="3" Height="18" 
                                           RadiusX="1.5" RadiusY="1.5" 
                                           Margin="0" 
                                           Opacity="0">
                                <Rectangle.Effect>
                                    <DropShadowEffect Color="{StaticResource AccentColorValue}" 
                                                          BlurRadius="8" ShadowDepth="0" Opacity="0.6"/>
                                </Rectangle.Effect>
                            </Rectangle>

                            <!-- 内容 -->
                            <StackPanel Orientation="Horizontal" Margin="{TemplateBinding Padding}">
                                <Border x:Name="IconContainer" Width="24" Height="24" 
                                            CornerRadius="6" Background="Transparent" Margin="0,0,14,0">
                                    <TextBlock x:Name="Icon" 
                                                   Text="{TemplateBinding Tag}" 
                                                   FontFamily="Segoe Fluent Icons" 
                                                   VerticalAlignment="Center" 
                                                   HorizontalAlignment="Center"
                                                   FontSize="14" 
                                                   Foreground="{TemplateBinding Foreground}"/>
                                </Border>
                                <ContentPresenter x:Name="ContentText" 
                                                      VerticalAlignment="Center" 
                                                      TextElement.Foreground="{TemplateBinding Foreground}"
                                                      TextElement.FontWeight="Normal"
                                                      TextElement.FontSize="13"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="#12FFFFFF"/>
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="#08FFFFFF"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="#08FFFFFF"/>
                            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 激活状态的导航按钮 -->
    <Style x:Key="NavButtonActiveStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="RootBorder" Background="#15FFFFFF" 
                                CornerRadius="8" BorderThickness="1" BorderBrush="#0CFFFFFF">
                        <Grid>
                            <Rectangle Fill="{StaticResource AccentBrush}" 
                                           HorizontalAlignment="Left" 
                                           Width="3" Height="18" 
                                           RadiusX="1.5" RadiusY="1.5">
                                <Rectangle.Effect>
                                    <DropShadowEffect Color="{StaticResource AccentColorValue}" 
                                                          BlurRadius="10" ShadowDepth="0" Opacity="0.8"/>
                                </Rectangle.Effect>
                            </Rectangle>
                            <StackPanel Orientation="Horizontal" Margin="{TemplateBinding Padding}">
                                <Border Width="24" Height="24" CornerRadius="6" Margin="0,0,14,0">
                                    <TextBlock Text="{TemplateBinding Tag}" 
                                                   FontFamily="Segoe Fluent Icons" 
                                                   VerticalAlignment="Center" 
                                                   HorizontalAlignment="Center"
                                                   FontSize="14" 
                                                   Foreground="{StaticResource AccentBrush}"/>
                                </Border>
                                <ContentPresenter VerticalAlignment="Center" 
                                                      TextElement.Foreground="{StaticResource TextPrimary}"
                                                      TextElement.FontWeight="SemiBold"
                                                      TextElement.FontSize="13"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== Fluent 卡片样式 ==================== -->
    <Style x:Key="FluentCard" TargetType="Border">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#24242A" Offset="0"/>
                    <GradientStop Color="#1C1C22" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="CornerRadius" Value="12"/>
        <Setter Property="BorderBrush" Value="#2F2F38"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="24"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect BlurRadius="24" ShadowDepth="2" Opacity="0.25" 
                                      Color="#000000" Direction="270"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 悬停卡片样式 -->
    <Style x:Key="FluentCardHoverable" TargetType="Border" BasedOn="{StaticResource FluentCard}">
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="#454550"/>
                <Setter Property="Background">
                    <Setter.Value>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                            <GradientStop Color="#2A2A32" Offset="0"/>
                            <GradientStop Color="#222228" Offset="1"/>
                        </LinearGradientBrush>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ==================== 按钮样式 ==================== -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
        <Setter Property="Foreground" Value="#0D0D0D"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Padding" Value="24,12"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect Color="{StaticResource AccentColorValue}" 
                                                  BlurRadius="16" ShadowDepth="0" Opacity="0.3"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#7DD6FF"/>
                            <Setter TargetName="ButtonBorder" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="{StaticResource AccentColorValue}" 
                                                          BlurRadius="24" ShadowDepth="0" Opacity="0.5"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#4DBCE6"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SecondaryButton" TargetType="Button">
        <Setter Property="Background" Value="#20FFFFFF"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6"
                                BorderBrush="#30FFFFFF"
                                BorderThickness="1"
                                Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#30FFFFFF"/>
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#40FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="#18FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== 进度条样式 ==================== -->
    <Style x:Key="FluentProgressBar" TargetType="ProgressBar">
        <Setter Property="Height" Value="6"/>
        <Setter Property="Background" Value="#25FFFFFF"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="3"/>
                        <Border x:Name="PART_Track" CornerRadius="3"/>
                        <Border x:Name="PART_Indicator" 
                                    HorizontalAlignment="Left" 
                                    CornerRadius="3">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                    <GradientStop Color="#60CDFF" Offset="0"/>
                                    <GradientStop Color="#38B0DE" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.Effect>
                                <DropShadowEffect Color="{StaticResource AccentColorValue}" 
                                                      BlurRadius="8" ShadowDepth="0" Opacity="0.5"/>
                            </Border.Effect>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== 滚动条样式 ==================== -->
    <Style x:Key="FluentScrollBar" TargetType="ScrollBar">
        <Setter Property="Width" Value="8"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Border Background="Transparent" Width="8" Margin="4,0">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="#40FFFFFF" 
                                                        CornerRadius="4" 
                                                        Margin="2,0"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== 分隔线样式 ==================== -->
    <Style x:Key="FluentSeparator" TargetType="Separator">
        <Setter Property="Height" Value="1"/>
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#00FFFFFF" Offset="0"/>
                    <GradientStop Color="#15FFFFFF" Offset="0.5"/>
                    <GradientStop Color="#00FFFFFF" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="Margin" Value="16,16"/>
    </Style>

    <!-- ==================== 窗口控制按钮 ==================== -->
    <Style x:Key="WindowControlButton" TargetType="Button">
        <Setter Property="Width" Value="46"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bg" Background="{TemplateBinding Background}">
                        <TextBlock Text="{TemplateBinding Content}" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"
                                       Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#20FFFFFF"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ==================== 导航按钮样式（基于RadioButton） ==================== -->
    <Style x:Key="NavRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
        <Setter Property="Height" Value="44"/>
        <Setter Property="Margin" Value="8,2"/>
        <Setter Property="Padding" Value="14,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="GroupName" Value="Navigation"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="RootBorder" 
                        Background="{TemplateBinding Background}" 
                        CornerRadius="8" 
                        BorderThickness="1" 
                        BorderBrush="Transparent">
                        <Grid>
                            <!-- 选中指示器 -->
                            <Rectangle x:Name="SelectionIndicator" 
                                   Fill="{StaticResource AccentBrush}" 
                                   HorizontalAlignment="Left" 
                                   Width="3" Height="18" 
                                   RadiusX="1.5" RadiusY="1.5" 
                                   Opacity="0">
                                <Rectangle.Effect>
                                    <DropShadowEffect Color="{StaticResource AccentColorValue}" 
                                                  BlurRadius="10" ShadowDepth="0" Opacity="0.8"/>
                                </Rectangle.Effect>
                            </Rectangle>

                            <!-- 内容 -->
                            <StackPanel Orientation="Horizontal" Margin="{TemplateBinding Padding}">
                                <Border Width="24" Height="24" CornerRadius="6" Margin="0,0,14,0">
                                    <TextBlock x:Name="Icon" 
                                           Text="{TemplateBinding Tag}" 
                                           FontFamily="Segoe Fluent Icons" 
                                           VerticalAlignment="Center" 
                                           HorizontalAlignment="Center"
                                           FontSize="14" 
                                           Foreground="{TemplateBinding Foreground}"/>
                                </Border>
                                <ContentPresenter x:Name="ContentText" 
                                              VerticalAlignment="Center" 
                                              TextElement.Foreground="{TemplateBinding Foreground}"
                                              TextElement.FontWeight="Normal"
                                              TextElement.FontSize="13"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!-- 鼠标悬停 -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="RootBorder" Property="Background" Value="#12FFFFFF"/>
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="#08FFFFFF"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                        </MultiTrigger>

                        <!-- 选中状态 -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="RootBorder" Property="Background" Value="#15FFFFFF"/>
                            <Setter TargetName="RootBorder" Property="BorderBrush" Value="#0CFFFFFF"/>
                            <Setter TargetName="SelectionIndicator" Property="Opacity" Value="1"/>
                            <Setter TargetName="Icon" Property="Foreground" Value="{StaticResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            <Setter TargetName="ContentText" Property="TextElement.FontWeight" Value="SemiBold"/>
                        </Trigger>

                        <!-- 选中状态下的悬停 -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="RootBorder" Property="Background" Value="#1AFFFFFF"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 添加到您的 ResourceDictionary 中 -->

    <!-- NullToVisibilityConverter（如果还没有的话） -->
    <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

    <!-- 可选：自定义 NullToVisibility 转换器需要在代码中实现 -->

    <!-- 增强的进度条样式变体 -->
    <Style x:Key="MemoryProgressStyle" TargetType="ProgressBar" BasedOn="{StaticResource FluentProgressBar}">
        <Setter Property="Height" Value="4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Grid>
                        <Border Background="#15FFFFFF" CornerRadius="2"/>
                        <Border x:Name="PART_Track" CornerRadius="2"/>
                        <Border x:Name="PART_Indicator" 
                            HorizontalAlignment="Left" 
                            CornerRadius="2"
                            Background="{StaticResource WarningBrush}"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 徽章样式 -->
    <Style x:Key="BadgeStyle" TargetType="Border">
        <Setter Property="Background" Value="#2060CDFF"/>
        <Setter Property="CornerRadius" Value="4"/>
        <Setter Property="Padding" Value="8,3"/>
    </Style>

    <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource WindowControlButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bg" Background="{TemplateBinding Background}">
                        <TextBlock Text="&#xE8BB;" 
                                       HorizontalAlignment="Center" 
                                       VerticalAlignment="Center"
                                       FontFamily="Segoe Fluent Icons"
                                       FontSize="10"
                                       Foreground="{TemplateBinding Foreground}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="#C42B1C"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>

// IronSight.App.UI.App.xaml
<Application x:Class="IronSight.App.UI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:IronSight.App.UI.ViewModels"
             xmlns:v="clr-namespace:IronSight.App.UI.Views"
             xmlns:local="clr-namespace:IronSight.App.UI"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>

// IronSight.App.UI.MainWindows.xaml
<Window x:Class="IronSight.App.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:IronSight.App.UI.ViewModels"
        xmlns:views="clr-namespace:IronSight.App.UI.Views"
        mc:Ignorable="d"
        Title="IronSight" Height="700" Width="1100"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        WindowStyle="ThreeDBorderWindow"
        FontFamily="Segoe UI Variable Display, Segoe UI, Microsoft YaHei UI"
        AllowsTransparency="False"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="ClearType"
        TextOptions.TextHintingMode="Fixed"
        RenderOptions.ClearTypeHint="Enabled"
        RenderOptions.BitmapScalingMode="HighQuality">
    <!--AllowsTransparency="True" -->

    <!-- 现代窗口框架 -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"
                      ResizeBorderThickness="8"
                      GlassFrameThickness="0"
                      CornerRadius="12"
                      UseAeroCaptionButtons="True"/>
    </WindowChrome.WindowChrome>

    <!--<WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0"
                      CornerRadius="0"
                      CaptionHeight="30" />
    </WindowChrome.WindowChrome> -->

    <!-- 主窗口容器 -->
    <Border CornerRadius="12"
            Background="{StaticResource PrimaryBackground}"
            BorderBrush="#35FFFFFF"
            BorderThickness="1">

        <Border.DataContext>
            <vm:MainViewModel />
        </Border.DataContext>

        <Border.Effect>
            <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.5" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- ==================== 侧边栏 ==================== -->
            <Border Background="{StaticResource SecondaryBackground}"
                    CornerRadius="12,0,0,12">
                <!-- 顶部微光效果 -->
                <Border.OpacityMask>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="White" Offset="0"/>
                        <GradientStop Color="White" Offset="1"/>
                    </LinearGradientBrush>
                </Border.OpacityMask>

                <Grid>
                    <!-- 顶部光泽 -->
                    <Border Height="100" VerticalAlignment="Top" CornerRadius="12,0,0,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                <GradientStop Color="#08FFFFFF" Offset="0"/>
                                <GradientStop Color="#00FFFFFF" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                    </Border>

                    <!-- 右边框线 -->
                    <Border HorizontalAlignment="Right" Width="1" Background="#20FFFFFF"/>

                    <DockPanel>
                        <!-- Logo区域 -->
                        <Border DockPanel.Dock="Top" Margin="24,24,24,32">
                            <StackPanel Orientation="Horizontal">
                                <Border Width="40" Height="40" CornerRadius="10">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#60CDFF" Offset="0"/>
                                            <GradientStop Color="#0078D4" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <TextBlock Text="&#xE7EE;"
                                               FontFamily="Segoe Fluent Icons"
                                               FontSize="18"
                                               Foreground="White"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Border>
                                <StackPanel Margin="14,0,0,0" VerticalAlignment="Center">
                                    <TextBlock Text="IRONSIGHT"
                                               FontSize="15"
                                               FontWeight="Bold"
                                               Foreground="{StaticResource TextPrimary}"
                                               />
                                    <TextBlock Text="系统监控"
                                               FontSize="11"
                                               Foreground="{StaticResource TextTertiary}"
                                               Margin="0,2,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- 底部设置按钮 -->
                        <StackPanel DockPanel.Dock="Bottom" Margin="0,0,0,16">
                            <Separator Style="{StaticResource FluentSeparator}"/>
                            <RadioButton x:Name="NavSettings" Content="设置" Tag="&#xE713;" CommandParameter="5" Style="{StaticResource NavRadioButtonStyle}" Checked="Nav_Checked"/>
                            <RadioButton x:Name="NavAbout" Content="关于" Tag="&#xE946;" CommandParameter="6" Style="{StaticResource NavRadioButtonStyle}" Checked="Nav_Checked"/>
                        </StackPanel>

                        <!-- 导航菜单 -->
                        <StackPanel Margin="0,0,0,0">
                            <TextBlock Text="导航" FontSize="11" Foreground="{StaticResource TextTertiary}" Margin="24,0,0,12" FontWeight="Medium"/>

                            <RadioButton x:Name="NavDashboard" Content="仪表盘" Tag="&#xE80F;" CommandParameter="0" Style="{StaticResource NavRadioButtonStyle}" IsChecked="True" Checked="Nav_Checked"/>
                            <RadioButton x:Name="NavClipboard" Content="剪贴板" Tag="&#xE8C8;" CommandParameter="1" Style="{StaticResource NavRadioButtonStyle}" Checked="Nav_Checked"/>
                            <RadioButton x:Name="NavMemory" Content="内存加速" Tag="&#xE950;" CommandParameter="2" Style="{StaticResource NavRadioButtonStyle}" Checked="Nav_Checked"/>
                            <RadioButton x:Name="NavProcess" Content="进程管理" Tag="&#xE7F4;" CommandParameter="3" Style="{StaticResource NavRadioButtonStyle}" Checked="Nav_Checked"/>
                            <RadioButton x:Name="NavNetwork" Content="网络监控" Tag="&#xE968;" CommandParameter="4" Style="{StaticResource NavRadioButtonStyle}" Checked="Nav_Checked"/>
                        </StackPanel>
                    </DockPanel>
                </Grid>
            </Border>

            <!-- ==================== 主内容区 ==================== -->
            <Grid Grid.Column="1">
                <!-- 自定义标题栏 -->
                <Border Height="48" VerticalAlignment="Top"
                        MouseDown="TitleBar_MouseDown"
                        Background="Transparent"
                        Panel.ZIndex="100">
                    <Grid>
                        <StackPanel Orientation="Horizontal" Margin="28,0,0,0" VerticalAlignment="Center">
                            <Ellipse Width="8" Height="8" Fill="{StaticResource SuccessBrush}"/>
                            <TextBlock Text="系统运行正常"
                                       VerticalAlignment="Center"
                                       Margin="10,0,0,0"
                                       FontSize="12"
                                       Foreground="{StaticResource TextTertiary}"/>
                        </StackPanel>

                        <!-- 窗口控制按钮 -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="&#xE921;" Style="{StaticResource WindowControlButton}" Click="Minimize_Click" Height="42"/>
                            <Button Content="&#xE922;" Style="{StaticResource WindowControlButton}" Click="Maximize_Click" Height="42"/>
                            <Button Style="{StaticResource CloseButton}" Click="Close_Click" Height="45"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- 页面内容 -->
                <TabControl x:Name="MainTabControl"
                            Margin="28,56,28,28"
                            BorderThickness="0"
                            Background="Transparent"
                            SelectedIndex="0">
                    <TabControl.Resources>
                        <Style TargetType="TabItem">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </Style>
                    </TabControl.Resources>

                    <TabItem>
                        <views:DashboardView/>
                    </TabItem>
                    <TabItem>
                        <views:ClipboardView/>
                    </TabItem>
                    <TabItem>
                        <views:MemoryCleanerView/>
                    </TabItem>
                    <TabItem>
                        <views:ProcessManagerView/>
                    </TabItem>
                    <TabItem>
                        <views:NetworkMonitorView/>
                    </TabItem>
                    <TabItem>
                        <views:SettingsView/>
                    </TabItem>
                    <TabItem>
                        <views:AboutView/>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </Border>
</Window>
